# dist-storage

Distributed storage components: **C++**, **gRPC**, **Protobuf**, **OpenSSL**. CMake build; see [`CMakeLists.txt`](CMakeLists.txt) for targets.

## Documentation

| Doc | Purpose |
|-----|--------|
| **[AGENTS.md](AGENTS.md)** | **Start here for AI agents and contributors** — stack layout, build commands, `guard_ptr`/OpenSSL notes, messaging and certificate pitfalls, workflow expectations |

Other code-level docs live next to the modules they describe (e.g. protos under `common/proto/`).
